import 'package:flutter/material.dart';

class AppColor {
  // Brand Colors
  static const primary = Color(
    0xFF2E6FF2,
  ); // Updated to match the UI's main blue
  static const secondary = Color(0xFF29A3FF);
  static const darkBlue = Color(0xFF0B4DFF);
  static const white = Color(0xFFFFFFFF);
  static const background = Color(0xFFF9FAFB);

  // Text Colors
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);
  static const textProfileDark = Color(0xFF4A4A68);
  static const textProfileLight = Color(0xFF7B7B9E);

  // Border & Dividers
  static const borderLight = Color(0xFFF3F4F6);
  static const borderMedium = Color(0xFFE5E7EB);
  static const borderDark = Color(0xFFD1D5DB);

  // Semantic Colors (Icons & Backgrounds)
  static const error = Color(0xFFDC2626);
  static const errorBg = Color(0xFFFEE2E2);
  static const success = Color(0xFF16A34A);
  static const successBg = Color(0xFFDCFCE7);
  static const warning = Color(0xFFD97706);
  static const warningBg = Color(0xFFFEF3C7);
  static const info = Color(0xFF4F46E5);
  static const infoBg = Color(0xFFE0E7FF);

  // Gradients
  static const gradientStart = Color(0xFF5384F5);
  static const gradientEnd = Color(0xFF3F69F3);
}
